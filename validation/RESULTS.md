# 三关验证记录

日期：2026-09-10。对象：当前包的初见、回收、余量三关。

## 规则穷举：通过

`node tools/check-rules.cjs` 遍历全部可达合法状态，检查材料守恒、角色支撑和每个状态是否仍可到达终点。

| 关卡 | 可达状态 | 最短规则动作 | 无死局 |
|---|---:|---:|---|
| 初见 | 15 | 8 | 通过 |
| 回收 | 59 | 10 | 通过 |
| 余量 | 17 | 9 | 通过 |

共 91 个状态。第二关需要回收与放置，第三关需要重复利用；奖励名称、非法操作、超范围编号拒绝通过。

## 模拟控制器：通过

`node tools/check-controller.cjs` 使用已注册点击事件和模拟计时，连续完成三关。检查结尾奖励、结束后不再转场、三项菜单、重玩、存档恢复、重置和无效编号回退。没有将通关状态直接注入控制器。

这是模拟 DOM 检查，不是真实浏览器端到端测试。未声明本次浏览器视觉、手机触摸、读屏或玩家理解验证通过。

## 完整性

`SOURCE_SNAPSHOT.json` 记录当前文件快照，`SHA256SUMS` 记录包内文件的当前哈希。使用 `python3 tools/check-integrity.py` 验证。旧维护记录中的验证结论不替代本次检查。
